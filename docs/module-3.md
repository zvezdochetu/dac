# Модуль 3. Ветвление и другие возможности Git

## Основные понятия этого модуля - TODO

## Золотые правила работы с гитом - TODO

Железный запрет на секреты и ключи

Ваш главный компас: git status

Не копить локальные изменения

Атомарность коммитов

Понятные сообщения коммитов

Безопасная работа с ветками

## Шпаргалка по командам

??? info "Отмена локальных изменений (Restore)"

    Если вы по ошибке изменили файл в локальном репозитории, можно вернуть его в исходное состояние:

    - Если измененный файл не был сохранен, нажимайте **Ctrl+Z** до полной отмены изменений, а затем сохраните файл (**Ctrl+S**).

    - Если измененный файл уже был сохранен, выполните команду:

        ```
        git restore <имя_файла>
        ```

        !!! tip "Совет"

            Чтобы отменить изменения во всех файлах, выполните команду:

            ```
            git restore .
            ```

    - Если измененный файл был не только сохранен, но и добавлен на стейдж, последовательно выполните команды:

        ```
        git restore --staged <имя_файла>
        ```
        ```
        git restore <имя_файла>
        ```

??? info "Стейдж (Staging Area)"

    Если один или несколько файлов изменены и сохранены, их можно отправить на стейдж (Staging Area).

    Добавление файла на стейдж:

    ```
    git add <имя_файла>
    ```

    Добавление всех измененных файлов на стейдж:

    ```
    git add .
    ```

    Удаление файла со стейджа:

    ```
    git restore --staged <имя_файла>
    ```

    Удаление всех файлов со стейджа:

    ```
    git restore --staged .
    ```

??? info "Коммит (Commit)"

    !!! warning "Внимание"

        Не делайте коммиты в ветку `main` и не вливайте в нее другие ветки. 

    Создать коммит:

    ```
    git commit -m "заголовок коммита"
    ```

    Исправить заголовок последнего локального коммита:

    ```
    git commit --amend -m "правильный заголовок коммита"
    ```

    Удалить коммит из текущей ветки и вернуть все изменения на стейдж:

    ```
    git reset --soft HEAD~1
    ```

??? info "Отправка (Push) и вытягивание (Pull) изменений с сервера"

    Если вы создали новую локальную ветку и сделали первый коммит, для его отправки на сервер выполните:

    ```
    git push -u origin <имя_ветки>
    ```

    После первого `push` ветка на сервере уже создана и привязана. Поэтому далее используйте краткую форму:

    ```
    git push
    ```

    Чтобы обновить локальную ветку `main` («принести состояние с сервера»), переключитесь на нее и выполните:

    ```
    git pull
    ```

??? info "Работа с ветками"

    !!! warning "Внимание"

        1. Перед созданием новой ветки проверьте, что вы находитесь на ветке `main`.
        1. Перед переключением ветки сохраните все изменения и сделайте коммит (или отмените изменения).

    Просмотр информации о локальных ветках:

    ```
    git branch
    ```

    Создание ветки:

    ```
    git switch -c <имя_ветки>
    ```

    Переключение на другую ветку:

    ```
    git switch <имя_ветки>
    ```

    Чтобы переключиться на предыдущую ветку, можно использовать краткий синтаксис:

    ```
    git switch -
    ```

    Для безопасного удаления ветки переключитесь на ветку `main` и выполните:

    ```
    git branch -d <имя_ветки>
    ```

    Для принудительного удаления ветки, в которой остались невлитые коммиты, переключитесь на ветку `main` и выполните:

    ```
    git branch -D <имя_ветки>
    ```

    Для вливания (Merge) ветки `main` в текущую:

    ```
    git merge main
    ```

    Отмена слияния:

    ```
    git merge --abort
    ```

??? danger "Отмена и сброс (если что-то пошло совсем не так)"

    Если вы столкнулись с ошибкой, которую не можете разрешить, можно сбросить локальный репозиторий. При этом локальный репозиторий в точности скопирует состояние удаленного репозитория.

    Это экстренная мера, удаляющая все ваши локальные изменения.
    На удаленный репозиторий это не повлияет.

    Чтобы выполнить сброс локального репозитория, последовательно выполните команды:

    ```
    git fetch origin
    ```
    ```
    git reset --hard origin/main
    ```

## Давайте начнем

Перед началом практической части проделайте следующие шаги:

1. Запустите VS Code и откройте в нем папку [склонированного репозитория docker-docs-course](prerequisites.md#clone) (меню **File -> Open Folder**, затем выберите `docker-docs-course`).

1. Если в VS Code закрыт встроенный терминал, откройте его (правой кнопкой мыши на папке `docker-docs-course`, **Open in Integrated Terminal**).

    ??? info "Как понять, что всё ок"

        В проводнике VS Code вы видите:

        ```
        docker-docs-course\
        _vale
        _vendor
        ...
        ```

        В терминале внизу:

        ```
        <путь_к_папке>\dac\docker-docs-course>
        ```

        При запуске в терминале команды `git status`:

        ```
        On branch main
        Your branch is up to date with 'origin/main'.

        nothing to commit, working tree clean
        ```

1. Откройте в браузере [страницу репозитория на GitHub](https://github.com/zvezdochetu/docker-docs-course).

1. Откройте в соседней вкладке браузера [сгенерированный сайт документации](https://zvezdochetu.github.io/docker-docs-course/).

## Практика 1: анатомия промышленного репозитория документации

1. Просмотрите репозиторий `docker-docs-course` в проводнике VS Code.
    
    Обратите внимание на следующие файлы и папки:

    * файл `.gitignore` и папка `.github` — обязательные элементы любого репозитория на GitHub;
    * папка `_vale` и файл `.vale.ini` — на проекте используется линтер Vale;
    * `hugo.yaml` — конфигурационный файл генератора статических сайтов Hugo;
    * `CONTRIBUTING.md` — признак OSS-проекта.

1. Просмотрите организацию разделов в папке `content`.

1. Откройте в браузере страницу [Docker Build Cloud setup](https://zvezdochetu.github.io/docker-docs-course/build-cloud/setup/).

    Чтобы найти соответствующий ей md-файл, скопируйте путь к странице без последнего слеша (`build-cloud/setup`) и вставьте его в строку Command Center (Центр команд) в верхней части окна VS Code. 

1. Посмотрите, как в открытом md-файле оформлены метаинформация, адмонишены и табы.

## Практика 2: отправка на стейдж

> Стейдж () — это специальная область

1. Проверьте состояние Git (`git status`).

1. Откройте в VS Code два md-файла, из которых собираются страницы [Docker Build Cloud setup](https://zvezdochetu.github.io/docker-docs-course/build-cloud/setup/) и [Uninstall Docker Desktop](https://zvezdochetu.github.io/docker-docs-course/desktop/uninstall/). Для этого используйте строку Command Center.

1. Измените открытые файлы `content\manuals\build-cloud\setup.md` и `content\manuals\desktop\uninstall.md` и сохраните изменения.

1. Проверьте, как изменилось состояние Git (`git status`).

1. Отправьте оба измененных файла на стейдж:

    ```
    git add .
    ```

1. Еще раз проверьте, как изменилось состояние Git.

1. Удалите файл `uninstall.md` со стейджа:

    ```
    git restore --staged content\manuals\desktop\uninstall.md
    ```

1. И опять проверьте состояние Git :)

1. Откатите изменения в файле `uninstall.md`:

    ```
    git restore content\manuals\desktop\uninstall.md
    ```

1. Ну и как обычно, проверьте состояние Git :)

1. Откройте вкладку Source Control на левой панели и проверьте, что файл `setup.md` отображается в области Staged Changes. Уберите его со стейджа (правая кнопка мыши, **Unstage Changes**), а затем снова поместите на стейдж (правая кнопка мыши, **Stage Changes**).

**Шпаргалка по командам:**

??? info "Отмена локальных изменений (Restore)"

    Если вы по ошибке изменили файл в локальном репозитории, можно вернуть его в исходное состояние:

    - Если измененный файл не был сохранен, нажимайте **Ctrl+Z** до полной отмены изменений, а затем сохраните файл (**Ctrl+S**).

    - Если измененный файл уже был сохранен, выполните команду:

        ```
        git restore <имя_файла>
        ```

        !!! tip "Совет"

            Чтобы отменить изменения во всех файлах, выполните команду:

            ```
            git restore .
            ```

    - Если измененный файл был не только сохранен, но и добавлен на стейдж, последовательно выполните команды:

        ```
        git restore --staged <имя_файла>
        ```
        ```
        git restore <имя_файла>
        ```

??? info "Стейдж (Staging Area)"

    Если один или несколько файлов изменены и сохранены, их можно отправить на стейдж (Staging Area).

    Добавление файла на стейдж:

    ```
    git add <имя_файла>
    ```

    Добавление всех измененных файлов на стейдж:

    ```
    git add .
    ```

    Удаление файла со стейджа:

    ```
    git restore --staged <имя_файла>
    ```

    Удаление всех файлов со стейджа:

    ```
    git restore --staged .
    ```

## Практика 3: создание коммита





## Практика 4: история коммитов

TODO

## Практика 5: создание и переключение веток

TODO

## Практика 6: отправка изменений на сервер


## Практика 7: слияние веток и разрешение конфликтов


## Важные замечания - TODO

- Ветка main может называться по-другому
- Более того, ее имя на сервере и у вас может отличаться
- В гите есть несколько способов сделать одно и то же
- VS Code тоже имеет в интерфейсе разные кнопочки, чтобы автоматизировать процесс работы с репозиторием
- Не факт, что вам понадобятся именно эти команды - в реальном проекте есть свои строгие правила относительно команд
- Точно так же, есть правила относительно именования веток и коммитов
- Есть много разных плагинов для VS Code, которые упрощают работу с ветками
- githook и локальный запуск линтеров: контроль
- золотые правила коммитов
